import {StyleSheet, Platform, StatusBar} from "react-native";

export const Global = StyleSheet.create({
    imageSize: {
        height: 150,
        width: 'auto',
        resizeMode: 'contain',
    },
})